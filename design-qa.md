# 界面视觉验收

## 接入详情

- 参考界面：访问监控详情抽屉（用户指定的现有视觉基准）。
- 实现界面：接入记录详情抽屉。
- 对照状态：两边均打开一条真实感配对失败记录，统一使用 1280 × 720 视口并置比较。
- 桌面布局：顶部四摘要卡、诊断卡、双列信息卡、全宽网络/记录区域及底部记录数据层级一致。
- 字体与间距：标题、辅助文字、字段标签、卡片内边距、区块间距和边框圆角一致。
- 状态与颜色：沿用现有 Badge 语义色与设计系统 token，没有新增独立视觉规则。
- 图标：推广、账号和网络信息均使用项目已有 Lucide 图标，尺寸和对齐方式一致。
- 内容：接入详情展示服务端安全留存的请求上下文、路由快照和处理结果；明确不展示验证码、代理凭据、令牌或完整请求体。
- 响应式：768 × 800 窄屏下抽屉、卡片、长字段和 JSON 数据无横向溢出或遮挡。
- 交互：详情打开、抽屉滚动、关闭按钮及落地页操作保持可用；两个页面控制台均无 error/warn。
- 自动验证：Web 生产构建通过；API `test_business_modules.py` 通过（22 passed）。

## 账号管理头像列与对齐

- 参考截图：`/var/folders/53/1ns7m50s5s31kg3wcfpk0bdm0000gn/T/codex-clipboard-d4e736fe-693d-4146-a731-0d9955407e29.png`（5120 × 2818，浏览器 2x 截图）。
- 实现截图：`/tmp/parloq-account-management-avatar-second-column.png`（2369 × 1409，本地开发页）。
- 并置对照：`/tmp/parloq-account-management-reference-comparison.png`；参考图按 CSS 视口缩放，实施图保持比例补白后左右并置。
- 对照状态：账号管理默认列表、4 条现有本地测试账号、筛选项未展开。
- 列顺序：复选框之后依次为“账号、头像、来源、分组、代理、账号数据、操作”；头像是用户口径中的第二列，不再嵌入账号主列。
- 头像样式：列表头像为 40 × 40 像素、`rounded-lg`（10px 圆角方形）；头像详情沿用相同圆角语义并放大为 64 × 64 像素。
- 对齐：来源与账号数据的标题、单元格及内部多行内容统一水平居中；账号主列保留原有左对齐和状态标识。
- 响应式：表格继续沿用项目已有横向滚动与固定操作列行为，没有修改落地页或其他业务页布局。
- 自动验证：Web TypeScript 与 Vite 生产构建通过；`git diff --check` 通过；浏览器 DOM 确认表头顺序为“账号、头像、来源…”，控制台无 error。

final result: passed
